{
  "name": "wildberries-select-all",
  "version": "0.0.1",
  "description": "",
  "main": "inject-select-all.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack ./index.js --mode production",
    "clean": "rm -rf build extension.zip release",
    "pack": "npm run build && zip extension.zip manifest.json dist/*",
    "release": "release-it"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lodash.find": "^4.6.0",
    "release-it": "^7.4.7",
    "url": "^0.11.0",
    "webpack": "^4.12.0",
    "webpack-cli": "^3.0.8"
  }
}
